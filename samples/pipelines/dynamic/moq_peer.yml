# SPDX-FileCopyrightText: © 2025 StreamKit Contributors
#
# SPDX-License-Identifier: MPL-2.0

name: MoQ Peer Transcoder (Gateway)
description: Accepts a WebTransport client via transport::moq::peer, applies gain, and loops processed audio back to subscribers
category: Streaming
tags:
  - streaming
  - transcoding
keywords:
  - moq
  - webtransport
  - gateway
mode: dynamic
client:
  gateway_path: /moq
  publish:
    broadcast: input
    tracks:
      - kind: audio
        source: microphone
  watch:
    broadcast: output
    audio: true
    video: false

nodes:
  # Bidirectional node that accepts WebTransport connections and provides:
  # - `audio/data`: OpusAudio from the publishing client
  # - `video/hd`: EncodedVideo (VP9) from the publishing client (when enabled)
  # - `in`:  OpusAudio to send to subscribing clients
  moq_peer:
    kind: transport::moq::peer
    params:
      gateway_path: /moq
      input_broadcasts:
        - input
      output_broadcast: output
      allow_reconnect: true
    # Loopback: send processed audio back into the peer for distribution to subscribers.
    # This is an intentional bidirectional cycle (peer `out` -> decode/process -> encode -> peer `in`).
    needs: opus_encoder

  opus_decoder:
    kind: audio::opus::decoder
    needs:
      in: moq_peer.audio/data

  gain:
    kind: audio::gain
    params:
      gain: 1.0
    needs: opus_decoder

  opus_encoder:
    kind: audio::opus::encoder
    needs: gain
