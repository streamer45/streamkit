# SPDX-FileCopyrightText: © 2025 StreamKit Contributors
#
# SPDX-License-Identifier: MPL-2.0

[package]
name = "source-plugin"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
publish = false

[lib]
name = "source_plugin"
crate-type = ["cdylib"]

[dependencies]
streamkit-plugin-sdk-native = { path = "../../../sdks/plugin-sdk/native" }
serde_json = "1.0"
# Required transitively: the `native_source_plugin_entry!` macro
# expands to code that references `tracing::` directly.
tracing = "0.1"
